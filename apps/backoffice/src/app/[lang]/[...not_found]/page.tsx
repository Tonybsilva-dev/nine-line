import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Not found",
  description: "Not found page",
};

export { NotFoundContainer as default } from "@/domains/(overview)/[...not_found]/container/not-found.container";
